import React from 'react';
import { Button } from '@chakra-ui/react';
import Link from 'next/link';

const CourseCard = ({ course }) => {
  return (
    <div>
      {/* Course card content here */}
      <Button variant="ghost" size="sm" asChild>
        <Link href={`/courses/${course.id}`}>Details</Link>
      </Button>
    </div>
  );
};

export default CourseCard;

// app/page.tsx
import React from 'react';
import Image from 'next/image';
import Link from 'next/link';

const HomePage = () => {
  return (
    <div>
      <header>
        <Link href="/" aria-label="AI Learning Planner Home" className="flex-shrink-0">
          <Image
            src="/logo.png"
            width={48}
            height={48}
            alt="AI Learning Planner Logo"
            className="rounded"
          />
        </Link>
        {/* Other header content here */}
      </header>
      <main>
        {/* Main content here */}
      </main>
    </div>
  );
};

export default HomePage;
